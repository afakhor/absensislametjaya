name: absensi_tb_slamet_jaya
description: Absensi TB. Slamet Jaya - Sistem Gaji Mingguan & Laba Bersih
publish_to: 'none'
version: 4.0.0+10

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  drift: ^2.18.0
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.2
  local_auth: ^2.1.8
  flutter_secure_storage: ^9.0.0
  intl: ^0.19.0
  image_picker: ^1.0.7
  permission_handler: ^11.3.1
  # PDF CETAK LOG LIVE & ABSENSI
  pdf: ^3.11.1
  printing: ^5.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.18.0
  build_runner: ^2.4.7
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # kalau ada foto karyawan, buka ini:
  # assets:
  #   - assets/images/